# OpenAkita 三层记忆系统调研报告
> 基于源码追踪，精确到代码行号。追踪范围：`memory/extractor.py`、`memory/manager.py`、`memory/retrieval.py`、`prompt/builder.py`、`core/_agent_runtime.py`。
>

---

## 一、整体架构
OpenAkita 将记忆严格划分为 **工作记忆（Scratchpad）**、**语义记忆（Semantic Memory）** 和 **情节记忆（Episode Memory）** 三层。三者的写入时机、提取机制及执行主体对比：

| 记忆类型 | 写入/生成时机 | 提取/召回时机 | 驱动方式 | 存储形式与核心作用 |
| :--- | :--- | :--- | :--- | :--- |
| **工作记忆 Scratchpad** | ① 意图分析后即时同步   ② 会话结束时深度格式化 | 每轮对话组装 Prompt 时直接读取 | **系统提取 + LLM 辅助** | Markdown 便签本，存放 `current_focus`、`active_projects`、`open_questions`、`next_steps` |
| **语义记忆 Semantic Memory** | ① 主题切换（Topic Change）   ② 会话结束（Session End）   ③ 上下文压缩前（Quick Facts） | 每轮对话前通过 `RetrievalEngine` 多路召回 | **LLM 提取为主 + 系统规则兜底** | 结构化实体-事实库（主语-谓语-内容），跨会话持久化 |
| **情节记忆 Episode Memory** | 仅在会话结束（`end_session`） | 每轮检索时通过实体关联召回历史情节 | **LLM 总结 + 系统动作节点捕获** | 完整交互故事线（目标、工具调用、结果、错误路径） |


---

## 二、工作记忆（Scratchpad / 便签本）
### 2.1 触发时机
#### 时机一：意图分析后即时同步（系统提取）


**文件**：`openakita/src/openakita/core/_agent_runtime.py`  
**行号**：4879–4899

```python
# 9.7 同步更新 Scratchpad 当前任务 (skip for CHAT intent to avoid overwriting task focus)
_new_task = compiler_summary or message[:200]
if _new_task and intent_result.intent != IntentType.CHAT:
    _sp_store = getattr(self.memory_manager, "store", None)
    if _sp_store:
        _pad = _sp_store.get_scratchpad() or _Sp()
        _old_focus = _pad.current_focus
        if topic_changed and _old_focus:
            # 话题切换时，把旧 focus 归档到 active_projects
            _pad.active_projects = (
                [f"[{datetime.now().strftime('%m-%d %H:%M')}] {_old_focus}"]
                + _pad.active_projects
            )[:5]
        _pad.current_focus = _new_task          # ← 直接写入 current_focus
        _pad.content = _pad.to_markdown()
        _pad.updated_at = datetime.now()
        _sp_store.save_scratchpad(_pad)
```

**触发条件**：IntentAnalyzer 分析完成后，只要不是 `CHAT` 闲聊意图，就将 `task_definition`（或**消息前 200 字符**）写入 Scratchpad。

#### 时机二：会话结束（LLM 深度格式化）
深度：

<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">LLM 接收：</font>

+ **<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">旧便签本</font>**<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">（</font>`<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">current_scratchpad</font>`<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">）</font>
+ **<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">最新情节摘要</font>**<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">（</font>`<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">episode.summary</font>`<font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">）</font>

## <font style="color:rgb(59, 59, 59);background-color:rgb(248, 248, 248);">会话结束的触发路径</font>
### <font style="color:rgb(59, 59, 59);background-color:rgb(248, 248, 248);">后端主导（非前端控制）</font>
| <font style="color:rgb(59, 59, 59);">触发时机</font> | <font style="color:rgb(59, 59, 59);">说明</font> | <font style="color:rgb(59, 59, 59);">代码位置</font> |
| :--- | :--- | :--- |
| **<font style="color:rgb(59, 59, 59);">单轮对话完成</font>** | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">chat_with_session</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">正常返回结果</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">_agent_runtime.py:5773</font>` |
| **<font style="color:rgb(59, 59, 59);">流式对话完成</font>** | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">chat_with_session_stream</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">正常返回</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">_agent_runtime.py:5773</font>` |
| **<font style="color:rgb(59, 59, 59);">异常退出</font>** | <font style="color:rgb(59, 59, 59);">上述方法抛异常，走 finally</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">_agent_runtime.py:5781</font>` |
| **<font style="color:rgb(59, 59, 59);">Agent 完全关闭</font>** | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">shutdown()</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">被调用</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">_agent_runtime.py:8554</font>` |
| **<font style="color:rgb(59, 59, 59);">无进展超时</font>** | <font style="color:rgb(59, 59, 59);">Orchestrator 检测</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">idle_timeout</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">触发</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">orchestrator.py:952</font>` |
| **<font style="color:rgb(59, 59, 59);">Agent 实例回收</font>** | <font style="color:rgb(59, 59, 59);">AgentFactory 回收闲置 Agent</font> | `<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">factory.py:664</font>` |


### <font style="color:rgb(59, 59, 59);background-color:rgb(248, 248, 248);">前端/用户可触发的停止</font>
| <font style="color:rgb(59, 59, 59);">触发方式</font> | <font style="color:rgb(59, 59, 59);">说明</font> | <font style="color:rgb(59, 59, 59);">实际行为</font> |
| :--- | :--- | :--- |
| <font style="color:rgb(59, 59, 59);">发送</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">/stop</font>`<br/><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">/停止</font>`<br/><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">/cancel</font>` | <font style="color:rgb(59, 59, 59);">IM 通道停止指令</font> | <font style="color:rgb(59, 59, 59);">取消当前任务 → 对话流结束 → 后端触发</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">end_session</font>` |
| <font style="color:rgb(59, 59, 59);">发送</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">取消</font>`<br/><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">停止</font>`<br/><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">abort</font>` | <font style="color:rgb(59, 59, 59);">自然语言停止</font> | <font style="color:rgb(59, 59, 59);">同上</font> |
| <font style="color:rgb(59, 59, 59);">调用</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">DELETE /api/sessions/{id}</font>` | <font style="color:rgb(59, 59, 59);">前端主动关闭会话</font> | <font style="color:rgb(59, 59, 59);">后端</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">close_session()</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">→</font><font style="color:rgb(59, 59, 59);"> </font>`<font style="color:rgb(59, 59, 59);background-color:rgba(0, 0, 0, 0.12);">on_session_end</font>`<br/><font style="color:rgb(59, 59, 59);"> </font><font style="color:rgb(59, 59, 59);">hook</font> |


  
 

**文件**：`openakita/src/openakita/memory/extractor.py`  
**行号**：743–782（`MemoryExtractor.update_scratchpad`）

仅在 `MemoryManager.end_session()` → `_finalize_session()` 后台异步任务中被调用。

### 2.2 LLM 提示词
**文件**：`openakita/src/openakita/memory/extractor.py`  
**行号**：148–168

```latex
你是 AI agent 的工作记忆管理器。基于最新的交互情节，更新工作记忆草稿本。

当前草稿本内容:
{current_scratchpad}

最新情节:
{episode_summary}

请输出更新后的完整草稿本 (Markdown 格式, 不超过 2000 字符):

## 当前项目
- ...

## 近期进展
- ...

## 未解决的问题
- ...

## 下一步
- ...
```

---

## 三、语义记忆（Semantic Memory / 长期事实与画像）
### 3.1 触发时机
#### 时机一：主题切换（Topic Change）
**前置检测**：  
**文件**：`openakita/src/openakita/core/_agent_runtime.py`  
**行号**：4818–4857

```python
topic_changed = False
_channel = getattr(session, "channel", None) if session else None
_is_im = _channel and _channel not in ("cli", "desktop")  # 仅 IM 通道
if _is_im and session and len(session_messages) >= 4:     # 对话历史 ≥ 4 轮
    topic_changed = await asyncio.wait_for(
        self._detect_topic_change(session_messages, message, session),
        timeout=10,
    )
    if topic_changed:
        # ... 插入上下文边界标记 ...
        # Fire-and-forget: 异步启动提取
        _extraction_task = _loop.create_task(
            self.memory_manager.extract_on_topic_change()   # ← 触发
        )
```

**三个前置条件必须同时满足**：

1. 会话通道为 IM（非 CLI/Desktop）
2. **<font style="color:#DF2A3F;">对话历史已有 ≥ 4 轮</font>**
3. LLM 判断当前消息与近期对话不相关

```markdown

IM 通道? (非 CLI/Desktop)
    │
    ├─── 否 ──→ 跳过话题检测 ──────────────────────────┐
    │                                                    │
    ├─── 是 ──→ 消息数 ≥ 4?                            │
                  │                                    │
                  ├─── 否 ──→ 跳过话题检测              │
                  │                                    │
                  ├─── 是 ──→ _detect_topic_change()    │
                              │                         │
                              ▼                         │
                  ┌─────────────────────────────────────┐
                  │ 第3次 LLM 调用: brain.compiler_think()│
                  │ 判断: CONTINUE 或 NEW                 │
                  └─────────────────────────────────────┘
                              │
                              ▼
                      话题切换检测结果
                              │
           ┌──────────────────┴──────────────────┐
           │                                     │
          是                                     否
           │                                     │
           ▼                                     ▼
把旧 current_focus              构建系统提示词
移到 active_projects
           │                                     │
           └─────────────┬───────────────────────┘
```

**提取执行**：  
**文件**：`openakita/src/openakita/memory/manager.py`  
**行号**：1330–1375

```python
async def extract_on_topic_change(self) -> int:
    turns = list(self._session_turns)
    if len(turns) < 3:       # 至少 3 轮对话才提取
        return 0
    cited = self._consume_cited_memories()
    items, scores = await asyncio.wait_for(
        self.extractor.extract_from_conversation(turns, cited_memories=cited or None),
        timeout=30.0,
    )
    # 保存提取的记忆，清空 turns 缓冲（新 topic 重新计数）
    self._session_turns.clear()
    return saved
```

#### 时机二：会话结束（Session End）
会话结束是语义记忆提取的最主要时机，在所有退出路径中都会被触发。

### <font style="color:rgb(32, 32, 32);background-color:rgb(250, 250, 253);">三种退出方式对比</font>
| <font style="color:rgb(32, 32, 32);">退出方式</font> | <font style="color:rgb(32, 32, 32);">原因</font> | <font style="color:rgb(32, 32, 32);">对应触发</font> |
| :--- | :--- | :--- |
| **<font style="color:rgb(32, 32, 32);">正常完成</font>** | <font style="color:rgb(32, 32, 32);">Agent 主动决定无需再调用工具，直接回复用户</font> | `<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">chat_with_session</font>`<br/><font style="color:rgb(32, 32, 32);"> </font><font style="color:rgb(32, 32, 32);">返回 →</font><font style="color:rgb(32, 32, 32);"> </font>`<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">_finalize_session</font>` |
| **<font style="color:rgb(32, 32, 32);">异常退出</font>** | <font style="color:rgb(32, 32, 32);">执行工具时报错、LLM 超时、内存不足等</font> | `<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">except</font>`<br/><font style="color:rgb(32, 32, 32);"> </font><font style="color:rgb(32, 32, 32);">捕获 → finally 调用</font><font style="color:rgb(32, 32, 32);"> </font>`<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">_finalize_session</font>` |
| **<font style="color:rgb(32, 32, 32);">强制取消</font>** | <font style="color:rgb(32, 32, 32);">用户发</font><font style="color:rgb(32, 32, 32);"> </font>`<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">/stop</font>`<br/><font style="color:rgb(32, 32, 32);">、无进展超时</font> | `<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">_cancel_session()</font>`<br/><font style="color:rgb(32, 32, 32);"> </font><font style="color:rgb(32, 32, 32);">→ 取消任务 →</font><font style="color:rgb(32, 32, 32);"> </font>`<font style="color:rgb(96, 96, 96);background-color:rgb(236, 236, 236);">_finalize_session</font>` |


**调用链**：

```plain
用户消息
    │
    ▼
┌─ Agent 推理循环 ──────────────────────────────────────┐
│                                                      │
│  LLM 思考 → 有工具调用？                              │
│    ├─ 是 → 执行工具 → 结果返回 LLM → 继续思考        │
│    │              ↑                                  │
│    │              └───── 循环 ──────────────────────┤
│    │                                                │
│    └─ 否（直接回复文本）→ 推理引擎返回  ←── 退出循环  │
│                                                      │
└──────────────────────────────────────────────────────┘
    │
    ▼
chat_with_session 正常完成
    │
    ▼
_finalize_session() → end_session() → 记忆提取



┌─────────────────────────────────────────────────────────────────┐
│                         触发源（5 种）                            │
├─────────────────────────────────────────────────────────────────┤
│  ① chat_with_session 正常完成  →  用户发送消息，得到回复，流程结束  │
│  ② chat_with_session 抛异常    →  执行出错，try/except 捕获        │
│  ③ chat_with_session_stream    →  流式响应，全部 token 发送完毕    │
│  ④ ask_user 等待用户确认       →  用户确认/取消后继续              │
│  ⑤ Agent.shutdown() 调用       →  Agent 实例被回收/服务关闭        │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│  _agent_runtime._finalize_session()  （Runtime 层共享收尾）        │
│                                                                 │
│  执行内容：                                                       │
│  1. 将推理过程摘要写入 session metadata                           │
│  2. 完成 TaskMonitor + 后台复盘                                 │
│  3. 调用 memory_manager.end_session()  ←── 关键：触发记忆提取     │
│  4. 清理临时状态（_current_task_definition 等）                   │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│  memory_manager.end_session()  （Memory 层会话结束入口）           │
│                                                                 │
│  5 种触发源最终都汇聚到这里：                                      │
│  • 第 5773 行：chat_with_session 正常完成                         │
│  • 第 5773 行：chat_with_session 异常（finally）                 │
│  • 第 5773 行：chat_with_session_stream 正常完成                  │
│  • 第 5773 行：chat_with_session_stream ask_user 退出            │
│  • 第 8554 行：Agent.shutdown() 最终清理                          │
│                                                                 │
│  此函数启动后台异步任务 _finalize_session()                        │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│  后台异步任务：_finalize_session()  （四个提取任务并行/串行执行）    │
│                                                                 │
│  ┌─ Task 1：generate_episode()      →  情节记忆（Episode）        │
│  │   LLM 总结对话目标/结果/涉及实体 + 系统捕获所有工具调用节点        │
│  │                                                                 │
│  ├─ Task 2：extract_from_conversation()  →  语义记忆 Track 1      │
│  │   用户画像提取（身份、偏好、行为规则）                            │
│  │   • 如果本次有被引用的历史记忆 → 同时做引用评分（useful?）        │
│  │                                                                 │
│  ├─ Task 3：extract_experience_from_conversation()  →  语义记忆 Track 2
│  │   任务经验提取（成功方法、失败教训、可复用配置）                   │
│  │                                                                 │
│  └─ Task 4：update_scratchpad()        →  工作记忆（Scratchpad）   │
│      基于 Episode 摘要重构便签本的四个板块                           │
└─────────────────────────────────────────────────────────────────┘

```

**关键点**：`end_session()` 在**所有退出路径**（正常完成 / 异常退出 / ask_user / shutdown）中都会被调用，确保记忆不丢失。

#### 时机三：上下文压缩前（Quick Facts）
**文件**：`openakita/src/openakita/memory/manager.py`  
**行号**：1904–1930

在上下文窗口即将压缩时触发，纯系统正则扫描：

```python
async def on_context_compressing(self, messages: list[dict]) -> None:
    """Called before context compression — extract quick facts and save to queue."""
    quick_facts = self.extractor.extract_quick_facts(messages)  # ← 系统正则
    # ...
```

扫描的正则规则（`extractor.py:816–822`）：

```python
_RULE_SIGNAL_PATTERNS = [
    re.compile(r"(?:每次|总是|always)\s*.{4,80}"),
    re.compile(r"(?:不要|不可以|禁止|never)\s*.{4,80}"),
    re.compile(r"(?:必须|务必|一定要|must)\s*.{4,80}"),
    re.compile(r"(?:永远|永远不要)\s*.{4,120}"),
    re.compile(r"(?:规则|rule)[：:]\s*.{4,120}"),
]
```

### 3.2 双轨提取机制（会话结束时）
会话结束时，语义记忆提取分为**两个独立轨道**，由同一个 `extract_from_conversation` 方法根据 `EXTRACTION_PROMPT_V2` 和 `EXPERIENCE_EXTRACTION_PROMPT` 分别驱动。

#### Track 1：用户画像提取
**Prompt**：`EXTRACTION_PROMPT_V2`  
**文件**：`openakita/src/openakita/memory/extractor.py`  
**行号**：55–132

核心原则：**区分「用户是谁」和「用户要做什么」**。记忆只存长期身份/偏好，不存一次性任务请求。

```latex
分析这轮对话，判断是否包含值得长期记住的信息。

对话内容:
[{role}]: {content}
{tool_context}
{extra_context}

### 核心原则：区分「用户是谁」和「用户要做什么」

**记忆只存「用户是谁」**（身份、性格、长期偏好），**不存「用户要做什么」**（任务、指令、请求）。

判断方法：问自己「这条信息在一个月后的新对话中还有用吗？」
- "用户喜欢简洁风格" → 有用 → 记录
- "用户需要苹果照片" → 没用（那是当时的任务） → 不记录
- "用户偏好通过Telegram接收通知" → 有用 → 记录
- "用户希望创建Word报告" → 没用（那是当时的任务） → 不记录

### 值得记录的（真正的长期信息）
- 用户身份：名字、称呼、职业、时区
- 用户性格偏好：沟通风格、语言习惯、审美取向
- 行为规则：用户对 AI 行为的持久要求（提炼为结构化规则）
- 技术环境：常用技术栈、开发工具、OS 信息
- 可复用经验：解决特定类型问题的通用方法
- 失败教训：需要长期避免的操作模式

### 绝对不要记录的
- **一次性任务请求**：「下载X」「搜索Y」「帮我找Z」「整理XX」「生成XX文档」
- **任务产物细节**：文件大小、分辨率、下载链接、具体报告内容
- **临时性需求**：「需要XX照片」「希望获取XX」「想要XX」
- 打招呼、寒暄、确认、感谢
- 系统状态、错误堆栈、调试信息
- AI 的回复内容、任务完成报告

### 常见误判示例
× "用户需要苹果和香蕉照片" → 这是任务请求，不是偏好！
× "用户希望在D盘创建报告" → 这是任务指令，不是规则！
✓ "用户偏好 Jarvis 人格风格" → 这是长期性格偏好
✓ "用户操作系统为Windows" → 这是持久环境事实
✓ "禁止虚报执行结果" → 这是行为规则

对于每条值得记录的信息，用 JSON 输出:
[
  {
    "type": "FACT|PREFERENCE|RULE|SKILL|ERROR",
    "subject": "实体主语 (谁/什么)",
    "predicate": "属性/关系 (偏好/版本/位于/...)",
    "content": "完整描述（精炼表达，不照抄原文）",
    "importance": 0.5-1.0,
    "duration": "permanent|7d|24h|session",
    "is_update": false,
    "update_hint": ""
  }
]

如果没有值得记录的信息, 只输出: NONE
```

#### Track 2：任务经验提取
**Prompt**：`EXPERIENCE_EXTRACTION_PROMPT`  
**文件**：`openakita/src/openakita/memory/extractor.py`  
**行号**：334–374

核心原则：**只记录可复用经验，不记录普通流水账**。

```latex
回顾整段对话，提取所有**任务经验、操作结果和教训**。

## 完整对话
{conversation}

### 核心原则：只记录可复用经验，不记录普通流水账

只记录下次遇到同类任务时能直接复用的成功方法、关键配置或踩坑教训。
普通执行过程、一次性产物和"已完成"类汇报不要写成长期经验。

### 必须记录的
- **成功的操作和方法**：什么操作最终成功了？用了什么配置/参数/步骤？（必须记录具体值）
- **失败的尝试和原因**：哪些方法失败了？报了什么错？原因是什么？
- **错误→修复的完整路径**：从错误到成功的关键转折（改了什么、为什么管用）
- **环境和配置发现**：调试过程中发现的系统特性、版本兼容性、端口、路径等
- **工具/Skill 使用经验**：用了哪个工具、怎么调用的、效果如何
- **Skill 封装经验**：创建了什么 skill、放在哪里、核心逻辑是什么、注意事项

### 不要记录的
- 打招呼、寒暄、感谢
- 用户身份信息（那属于用户画像记忆）

对于每条记录，用 JSON 输出:
[
  {
    "type": "EXPERIENCE|SKILL|ERROR",
    "subject": "主题 (什么任务/什么操作)",
    "predicate": "属性 (成功方法/失败原因/踩坑教训/Skill封装/最终配置/...)",
    "content": "详细描述（包含具体的参数、路径、配置值、错误信息，确保下次可直接复用）",
    "importance": 0.5-1.0,
    "duration": "permanent|7d"
  }
]

如果对话中确实没有任何操作或经验，输出: NONE
```

### 3.3 防污染机制（系统介入）
在 LLM 提取之后，系统执行多层保护：

1. **任务流水账过滤**（`manager.py:1144–1156`）：使用正则识别一次性任务动词（"帮我做"、"生成"、"搜索" 等），防止其被提升为 PERMANENT 永久记忆。
2. **多层去重**（`manager.py:1529–1572`）：
    - L1 精确哈希：完全相同的记忆直接跳过
    - L2 N-Gram 相似度：重叠率 > 80% 直接合并，> 30% 才调轻量 LLM 二次判定

---

## 四、情节记忆（Episode Memory / 故事线）
### 4.1 触发时机
**文件**：`openakita/src/openakita/memory/manager.py`  
**行号**：1671–1695

仅在会话结束时的后台异步任务中生成。

```python
async def _finalize_session():
    episode = None
    try:
        episode = await self.extractor.generate_episode(
            turns, session_id, source="session_end"   # ← 触发
        )
        if episode:
            # 关联 compaction checkpoint（如有）
            self.store.save_episode(episode)
    except Exception as e:
        logger.warning(f"[Memory] Episode generation failed: {e}")
```

### 4.2 混合提取机制
+ **系统提取（兜底）**：自动解析会话中所有工具调用记录，构造 `ActionNode` 节点列表（包含工具名、关键参数、执行结果、是否成功、错误信息、耗时）。
+ **LLM 总结**：生成高维度目标（`goal`）、执行结果（`outcome`）、涉及实体（`entities`）及摘要（`summary`）。如果 LLM 失败，系统通过首条消息和正则提取兜底。

### 4.3 LLM 提示词
**文件**：`openakita/src/openakita/memory/extractor.py`  
**行号**：134–146

```latex
基于以下对话轮次，生成一个情节摘要。

对话:
{conversation}

请用 JSON 格式输出:
{
  "summary": "一段话描述发生了什么 (100-200字)",
  "goal": "用户的目标/意图",
  "outcome": "success|partial|failed|ongoing",
  "entities": ["涉及的实体: 文件路径、项目名、概念等"],
  "tools_used": ["使用的工具名列表"]
}
```

---

## 五、记忆的检索与召回（How to Retrieve）
### 5.1 检索入口
组装 System Prompt 时由 `openakita/prompt/builder.py` 调用 `RetrievalEngine.retrieve()`。

### 5.2 多路召回通道
| 通道 | 来源表 | 匹配方式 |
| :--- | :--- | :--- |
| **语义通路** | `semantic_memories` | 向量检索 + SQLite FTS5 全文检索 |
| **情节通路** | `episodes` | 当前 Query 中的实体（文件路径、代码类名）与历史 Episode 关联 |
| **附件通路** | `attachments` | 媒体词匹配文件与图片元数据 |


### 5.3 LLM 查询拆解（Query Decomposition）
检索前，系统调用轻量级模型将自然语言拆解为搜索关键词：

**Prompt**：`QUERY_DECOMPOSE_PROMPT`  
**文件**：`openakita/src/openakita/memory/retrieval.py`  
**行号**：176–188

```latex
从用户消息中提取用于记忆检索的搜索关键词。

用户消息: {query}
{context_hint}

规则:
1. 提取核心实体、名称、主题词，去掉语气词/助词/代词
2. 如果涉及文件/图片/视频，提取描述性关键词（如"猫""报告"）和可能的文件名
3. 保留专有名词、技术术语原样
4. 输出 JSON: {"keywords": ["关键词1", "关键词2", ...], "intent": "search_memory|search_file|general"}
5. keywords 最多 6 个，每个 1-4 个词
只输出 JSON，不要其他内容。
```

### 5.4 综合打分重排序
$ \text{Score} = 0.40 \times \text{Relevance} + 0.20 \times \text{Recency} + 0.20 \times \text{Importance} + 0.20 \times \text{AccessFreq} $

冷启动豁免：刚写入 1 小时内的记忆即便综合分低于 0.35 也保留。

---

## 六、触发时序总图
```plain
用户消息到达
    │
    ▼
┌─ IntentAnalyzer.analyze()  ──→ 更新 Scratchpad.current_focus（系统写入）
│
├─ IM 通道 + 对话历史 ≥ 4 轮 ?
│       │
│       ├─ 是 → LLM 判断 topic_change
│       │         │
│       │         ├─ 话题切换 → extract_on_topic_change() → 写入 SemanticMemory
│       │         │                         → 清空 _session_turns
│       │         │
│       │         └─ 话题未切换 → 不提取
│       │
│       └─ 否（CLI 通道）→ 不检测 topic_change
│
├─ 执行推理 + 工具调用
│
└─ 对话结束（正常 / 异常 / ask_user / shutdown 任一路径）
          │
          ▼
    _finalize_session()  ← 所有退出路径共享
          │
          ▼
    memory_manager.end_session()
          │
          ├─ generate_episode()              → 写入 Episode（LLM 总结 + 系统节点捕获）
          ├─ extract_from_conversation()      → Track 1: 写入 SemanticMemory（用户画像）
          ├─ extract_experience...()          → Track 2: 写入 SemanticMemory（任务经验）
          └─ update_scratchpad()             → 更新 Scratchpad（LLM 格式化）
```

---

## 七、触发时机汇总表
| 记忆类型 | 触发时机 | 精确代码位置 | 驱动方式 |
| :--- | :--- | :--- | :--- |
| **Scratchpad** | 每轮对话分析后 | `_agent_runtime.py:4879` | 系统（`task_definition` 直接写入） |
| **Scratchpad** | 会话结束 | `manager.py:743` | LLM（`update_scratchpad`） |
| **SemanticMemory** | Topic Change 检测为真 | `manager.py:1330` | LLM（`extract_from_conversation`） |
| **SemanticMemory** | 会话结束 | `manager.py:1700` | LLM（双轨提取） |
| **SemanticMemory** | 上下文压缩前 | `manager.py:1904` | 系统（正则规则） |
| **Episode** | 会话结束 | `manager.py:1674` | LLM（`generate_episode`）+ 系统（`ActionNode`） |


---

## 八、相关源文件索引
| 文件 | 主要职责 |
| :--- | :--- |
| `memory/extractor.py` | 所有 LLM 提取提示词与提取逻辑 |
| `memory/manager.py` | 记忆管理器，协调触发时机与存储 |
| `memory/retrieval.py` | 检索引擎，多路召回与重排序 |
| `prompt/builder.py` | System Prompt 组装，调用检索 |
| `core/_agent_runtime.py` | 主运行时，触发 Scratchpad 更新与 topic_change 检测 |

